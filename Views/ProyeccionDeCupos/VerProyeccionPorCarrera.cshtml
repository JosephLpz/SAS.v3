@model IEnumerable<SAS.v1.Models.ProyeccionDeCupo>

@{
    ViewBag.Title = "Ver Proyecciones";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>VerProyeccionPorCarrera</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CuposProyectados)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CuposRestantes)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Asignatura.NombreAsignatura)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Carrera.NombreCarrera)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Anio.Ano)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CuposProyectados)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CuposRestantes)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Asignatura.NombreAsignatura)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Carrera.NombreCarrera)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Anio.Ano)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
                @Html.ActionLink("Details", "Details", new { id=item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.Id })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
